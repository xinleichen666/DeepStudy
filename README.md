# 研迹 DeepStudy

本地优先的研究生文献精读工具：导入一篇文献后，用大模型抽出基础知识点并向你提问；问答按知识点保存。再次打开时，**问过的概念会标在原文上**，点一下就能接着上次的对话继续问。

数据只存在本机，除你配置的模型接口外，不经过第三方服务器。

> **重要：请自行配置模型 API**  
> 本项目**不提供**任何可用的 API Key 或公共模型账号。抽取知识点、划词提问、对话讲解都依赖大模型，你必须在应用的「API」设置里填写**自己的**接口地址与 Key（如 DeepSeek、通义千问等）。  
> Key 只保存在你本机，不会上传到本仓库或其它中间服务器。没有配置 Key 时，仍可导入文献，但无法自动抽取与问答。

---

## 支持的文献格式

| 类型 | 说明 |
|------|------|
| **网页链接** | 论文页、技术博客、公众号长文等；用本机 Chrome / Edge 打开并抓取正文与配图，尽量保留原排版 |
| **arXiv** | 粘贴 `/abs/...` 会自动转到 PDF 再解析 |
| **PDF 文件** | 直接上传本地 PDF；需带可选中的文字层 |

**暂不支持**

- 扫描版 / 纯图片 PDF（无文字层，无法抽正文与定位）
- Word、EPUB、纯图片截图等其他文件格式
- 需登录才能看全文的付费墙页面（抓不到正文时请改用 PDF）

---

## 主要功能

1. **导入文献**  
   粘贴链接或上传 PDF，写入本地文库。

2. **知识点抽取**  
   调用国产 / OpenAI 兼容模型，抽出理解全文所需的基础概念，并为每条生成启发式提问。

3. **原文精读**  
   网页尽量保留原结构与配图；PDF 按页展示文本。问过的知识点用色块标在原文上。

4. **划词提问**  
   在原文中划选一段自由提问：能对应已有知识点则归入该条，否则自动新建知识点，不硬塞。

5. **悬浮问答**  
   点击知识点或标注打开可拖动、可调透明度的对话窗；支持精讲 / 例子 / 易混等快捷追问，回复支持 LaTeX。

6. **知识谱**  
   只收录真正问过的概念：来自哪篇文章、问了什么、还关联了哪些知识点，并可跳回原文标注。

7. **模型接口（需自备 Key）**  
   支持 DeepSeek、通义千问、智谱 GLM、Kimi、豆包，或任意 OpenAI Chat Completions 兼容网关。使用前请到服务商控制台自行申请 Key，并在应用内填写；**仓库与安装包均不包含可用 Key**。

8. **桌面应用**  
   可打包为 Windows 安装版 / 便携版，数据在本机用户目录。

---

## 快速开始

```bash
npm install
npm run dev
```

1. 浏览器打开 [http://localhost:3000](http://localhost:3000)。  
2. **先到「API」页填写你自己的模型服务商、接口地址与 API Key**（必做，否则无法抽取与提问）。  
3. 再导入链接或 PDF 开始精读。

推荐默认：**DeepSeek**（需自行注册并创建 Key）  
- 接口：`https://api.deepseek.com/v1`  
- 模型：`deepseek-chat`  
- 申请 Key：[platform.deepseek.com/api_keys](https://platform.deepseek.com/api_keys)

抓取网页需要本机已安装 **Chrome 或 Edge**。

---

## 桌面版

```bash
npm run app          # 开发窗口（必要时会启动本地服务）
npm run dist         # 打 Windows 包，输出在 release/
```

- `DeepStudy-*-Setup.exe`：安装版  
- `DeepStudy-*-portable.exe`：免安装  

开发时数据在项目下 `data/`；安装 / 便携版在 `%APPDATA%\DeepStudy\data`。菜单「研迹 → 打开数据目录」可直接打开。

首次打开桌面版同样要先在「导入 / API」里填入**你自己的** API Key，才能使用抽取与问答。

---

## 技术栈

Next.js · React · Electron · Playwright（网页抓取）· 本机 JSON 存储

---

## 许可

Private / 按仓库设置为准。
